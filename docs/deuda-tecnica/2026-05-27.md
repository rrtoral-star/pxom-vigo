## Fix viewbox Nominatim — geocoding calles homónimas

**Problema:** Nominatim devuelve calles homónimas en barrios periféricos
antes que las del centro (ej: García Barbón en Teis vs centro).

**Fix pendiente:** en geocoding.py, método geocode(), añadir viewbox al
geolocator.geocode():

    viewbox=[(-8.758, 42.255), (-8.685, 42.215)],
    bounded=False,

**Fichero:** app/utils/geocoding.py
**Prioridad:** media (workaround: usar nomenclatura gallega o RC directa)
