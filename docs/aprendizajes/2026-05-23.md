## Docling v2 — cambios de API
- `PipelineOptions` → ahora es `PdfPipelineOptions`
- `do_ocr=False` para PDFs nativos (evita descarga de modelos innecesarios)

## Chunker — ajustes pendientes para ORDET
- Fix patrón IMAGEN: `\[IMAGEN[_\s]\d+\]` cubre variantes con espacio
- Añadir `PATRON_SECCION_ORDET` para secciones tipo "1. NORMAS XERAIS"
- Añadir campos `sector` y `sector_nombre` al payload
- Tabla solo va al chunk i==0 — crear chunks dedicados tipo_chunk="tabla"
