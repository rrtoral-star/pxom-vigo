## Refactor página Catastral — v3.0

**Bugs corregidos:**
- `mostrar_zona_postgis()` no se capturaba → `zona_pxom_resumen` siempre era 'N/A' → bloque RAG nunca se ejecutaba
- `import os` faltaba en el bloque global de imports

**Cambios funcionales:**
- Chat conversacional (`st.chat_input`) reemplaza `st.text_input` + botón
- Memoria por parcela: `cat_context` (RC, ordenanza, sup, vía, usos) + `cat_messages` (historial)
- Reset de chat solo al cambiar de parcela, no al recargar
- Query al retriever enriquecida silenciosamente con datos de la parcela activa

**app.py:** memoria conversacional ya estaba integrada. Historial global (no se resetea al cambiar filtro PXOM/NHV/CTE) — decisión consciente, revisar si genera confusión en uso real.

---
*2026-05-27 22:32*

## Catastral V3.1 — fusión V2 + V3

**Problema:** el refactor a V3 perdió toda la riqueza del display de V2:
layout dos columnas, tabla de unidades, resumen por usos, expandible
para división horizontal, fallback callejero Catastro, link a sedecatastro,
ArcGIS como fuente secundaria.

**Solución:** fusión V3.1 que mantiene todo el display de V2 y añade
la memoria conversacional y chat interface de V3.

**Lección:** antes de refactorizar, hacer diff explícito contra la versión
anterior para no perder funcionalidad. El .bak fue clave para recuperarlo.

**Geocoding:** ejemplos cambiados a nomenclatura gallega (Rúa en vez de
Calle) para evitar ambigüedades con calles homónimas en barrios periféricos.
Fix pendiente: añadir viewbox centrado en casco urbano a Nominatim.
